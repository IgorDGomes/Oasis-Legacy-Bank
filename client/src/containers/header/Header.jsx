import React, { useState } from 'react';
import Navbar from '../../components/navbar/Navbar';

function Header() {
    return (
        <header>
            <Navbar />
        </header>
    )
}

export default Header;